pre-commit:
  jobs:
    - run: npx prettier -w -- {staged_files}
      glob: '*.{js,jsx,json,css,md}'
      stage_fixed: true

    - run: npx eslint -- {staged_files}
      glob: '*.{js,jsx}'
      stage_fixed: true
